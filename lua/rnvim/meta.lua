-- Version of the plugin. Must match the rnvim-agent release it deploys:
-- agent binaries are version-stamped on the remote and pulled from the
-- GitHub release with this same version (release-train discipline).
return { version = "0.10.2" }
